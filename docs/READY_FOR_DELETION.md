# ✅ 准备删除旧版本 - 最终确认

**日期**: 2025-12-15  
**状态**: ✅ **已准备就绪，可以开始删除流程**

---

## 📊 完成度确认

### ✅ 高优先级端点（前端依赖）

| 端点 | 状态 | 测试结果 | 前端组件 |
|------|------|---------|---------|
| `/api/market-anomaly/scan` | ✅ 已迁移 | HTTP 200 ✅ | MarketAnomalyScanner |
| `/api/system/status` | ✅ 已迁移 | HTTP 200 ✅ | ManagementDashboard |
| `/api/system/monitoring-stocks` | ✅ 已迁移 | HTTP 200 ✅ | ManagementDashboard |

**完成度**: ✅ **100% (3/3)**

### ✅ 中低优先级端点评估

| 端点 | 评估结果 | 决策 |
|------|---------|------|
| `/api/anomaly/strong-stocks` | ✅ 可废弃 | 前端数据已从其他端点获取 |
| `/api/limit-up/tracking` | ✅ 可废弃 | 未使用，未实现 |
| `/api/market-anomaly/latest` | ✅ 可废弃 | 功能已合并到 `market-anomaly/scan` |
| `/api/alert` (POST) | ✅ 可废弃 | 未使用，功能可替代 |

**结论**: ✅ **所有缺失端点均可废弃，不影响删除**

---

## ✅ 删除前检查清单

### 必须完成 ✅
- [x] 高优先级端点迁移完成
- [x] API端点测试通过
- [x] 中低优先级端点评估完成
- [ ] 前端功能测试通过 ⏳ **建议执行（15-30分钟）**
- [ ] 创建 v2.0.0 备份 ⏳ **待执行**

### 可选完成
- [ ] 补充 `/api/anomaly/strong-stocks`（如前端测试发现需要）

---

## 🚀 推荐执行计划

### 立即执行（今天，约1小时）

#### 步骤1: 前端功能测试 (15-30分钟) ⭐⭐⭐⭐⭐

**测试内容**:
1. 打开 http://localhost:3000
2. 测试 MarketAnomalyScanner 组件
3. 测试 ManagementDashboard - 系统状态
4. 测试 ManagementDashboard - 监控股票列表
5. 检查浏览器 Console 是否有错误

**预期结果**: 所有组件正常工作，无错误

#### 步骤2: 创建备份 (20分钟) ⭐⭐⭐⭐

```bash
# 创建备份目录
mkdir -p backups/backend-legacy-$(date +%Y%m%d)

# 备份 backend 目录
cp -r backend/main_modular.py backups/backend-legacy-$(date +%Y%m%d)/
cp -r backend/modules/ backups/backend-legacy-$(date +%Y%m%d)/

# Git提交
git add backups/backend-legacy-*/
git commit -m "feat: archive backend v2.0.0 before deletion"
git tag -a v2.0.0-archived -m "Backend modular version archived"
```

#### 步骤3: 删除 v2.0.0 (10分钟) ⭐⭐⭐⭐

```bash
# 安全删除（重命名方式）
mv backend backend.deleted.$(date +%Y%m%d)

# 或者直接删除
# rm -rf backend/main_modular.py
# rm -rf backend/modules/
```

#### 步骤4: 立即验证 (5分钟)

```bash
# 验证服务运行
curl http://localhost:8080/gateway/health
curl http://localhost:9001/health

# 验证端点
curl http://localhost:8080/api/market-anomaly/scan?limit=3
curl http://localhost:8080/api/system/status
curl http://localhost:8080/api/system/monitoring-stocks

# 验证前端
# 打开 http://localhost:3000 检查是否正常
```

---

## ⚠️ 风险控制

### 删除前检查
- ✅ 高优先级端点 100% 完成
- ✅ API测试通过
- ✅ 中低优先级端点评估完成
- ⏳ 前端功能测试（建议执行）

### 删除后监控
- **监控时长**: 3-5天
- **监控指标**: API响应时间、错误率、服务健康状态、前端功能
- **回滚计划**: 从 `backups/backend-legacy-*/` 恢复

---

## 📝 删除执行记录模板

```
删除时间: 2025-12-15 XX:XX
执行人员: [你的名字]

删除内容:
- backend/main_modular.py
- backend/modules/

备份位置: backups/backend-legacy-YYYYMMDD/

验证结果:
- Gateway健康: ✅/❌
- Signal API健康: ✅/❌
- 端点测试: ✅/❌
- 前端功能: ✅/❌

备注: [其他发现的问题]
```

---

**报告生成时间**: 2025-12-15  
**准备状态**: ✅ **已准备就绪**  
**建议**: 先执行前端功能测试（15-30分钟），然后开始删除流程

